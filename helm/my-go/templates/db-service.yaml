apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-go.fullname" . }}
  labels:
    {{- include "my-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-go.selectorLabels" . | nindent 4 }}
